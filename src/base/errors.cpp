#include "ccxt/base/errors.h"

// Since all error classes are implemented in the header file,
// this cpp file is intentionally left empty.
// It exists to maintain consistency with the project structure.
